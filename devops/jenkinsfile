pipeline {
    agent none
    stages {
        stage('Test') {
            agent {
                docker { image 'cypress/base:14.7.0' }
            }
            steps {
                sh "npx cypress run --env allure=true"
            }
        }
    }
}