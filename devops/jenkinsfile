pipeline {
    agent any

    environment {
        CHROME_BIN = '/bin/google-chrome'
    }
    stages {

        stage('e2e Tests') {
            steps {
                sh 'npm run test'
            }
        }
    }
}